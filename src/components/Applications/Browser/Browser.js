import Window from "/src/components/Window/Window";

import styles from "./Browser.module.css";

const Calculator = (props) => {
  return <Window {...props} title="Browser"></Window>;
};

export default Calculator;
